Quickstart
==========

After configuring input files, run ALF from the command line:

.. code-block:: bash

   python -m alframework --master master.json

See the examples section for concrete starter configurations.

